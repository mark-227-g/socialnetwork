const router = require('express').Router();
const {
//  removeReaction,
 // addReaction,
} = require('../../controllers/reactionController');

//router
//  .get(addReaction)
//  .put(removeReaction);

module.exports = router;